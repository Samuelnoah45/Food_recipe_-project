mutation reviews($comment:[comments_insert_input!]!,$rating:[rating_insert_input!]!,){
  insert_comments(objects:$comment){
    affected_rows
  }
  
  insert_rating(objects:$rating){
    affected_rows
  }
}
